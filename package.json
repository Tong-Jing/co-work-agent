{
  "name": "local-web-agent",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run build --workspace @local-agent/contracts && concurrently --kill-others -n web,server -c cyan,green \"npm --prefix apps/web run dev\" \"npm --prefix apps/agent-server run dev\"",
    "build": "npm run build --workspace @local-agent/contracts && npm run build --workspace @local-agent/server && npm run build --workspace @local-agent/web",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "test": "npm run test --workspaces --if-present"
  },
  "devDependencies": {
    "concurrently": "latest",
    "typescript": "^5.9.3"
  },
  "engines": {
    "node": ">=22.13.0",
    "npm": ">=10.0.0"
  }
}
